{{ 'collections-list.css' | asset_url | stylesheet_tag }}

<div class="collections-banner full-width" style="--heade-align:{{ section.settings.head_title_alignment }};">
  <h1 class="collections-header">{{ 'collections.title' | t }}</h1>
</div>

{% assign current_page_size = section.settings.page_size_default %}

{% paginate collections by current_page_size %}

<div
  class="collections {{ section.settings.grid_item_width }}"
  style="--grid-gap: {{ section.settings.grid_gap }}px; --padding-top:{{ section.settings.padding_top }}px; --padding-bottom:{{ section.settings.padding_bottom }}px;"
  >
  {% for collection in collections %}
    {% render 'collection-card', collection: collection, section: section %}
  {% endfor %}
</div>
<div class="pagination">
  {% render 'pagination', pagination: paginate %}
</div>
{% endpaginate %}



{% schema %}
{
  "name": "t:general.collections_grid",
  "settings": [
    {
      "type": "header",
      "content": "t:labels.collection_title"
    },
    {
      "type": "number",
      "id": "page_size_default",
      "label": "t:labels.items_per_page",
      "default": 4
    },
    {
      "type": "select",
      "id": "head_title_alignment",
      "label": "t:labels.alignment",
      "options": [
        { "value": "left", "label": "t:options.alignment.left" },
        { "value": "center", "label": "t:options.alignment.center" },
        { "value": "right", "label": "t:options.alignment.right" }
      ],
      "default": "center"
    },
    {
      "type": "header",
      "content": "t:general.collection_card"
    },
    {
      "type": "select",
      "id": "title_alignment",
      "label": "t:labels.alignment",
      "options": [
        { "value": "left", "label": "t:options.alignment.left" },
        { "value": "center", "label": "t:options.alignment.center" },
        { "value": "right", "label": "t:options.alignment.right" }
      ],
      "default": "center"
    },
    {
      "type": "color",
      "id": "card_background",
      "label": "t:labels.background",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "card_hover_color",
      "label": "t:labels.hover_color"
    },
    {
      "type": "header",
      "content": "t:labels.padding"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 36,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_top",
      "default": 20
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 36,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_bottom",
      "default": 20
    }
  ],
  "presets": [{ "name": "t:general.collections_grid" }]
}
{% endschema %}
